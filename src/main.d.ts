/**
 * `truncate-json` options
 */
export interface Options {}

/**
 *
 * @example
 * ```js
 * ```
 */
export default function truncateJson(
  jsonString: string,
  maxSize: number,
  options?: Options,
): string
